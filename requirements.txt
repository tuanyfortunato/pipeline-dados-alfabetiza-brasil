# Acesso ao BigQuery e Cloud Storage (sem precisar de Spark para testar)
google-cloud-bigquery==3.25.0
google-cloud-storage==2.18.2
db-dtypes==1.3.0
pandas==2.2.3

# Processamento distribuído (usado a partir da Fase 1 - Bronze)
pyspark==3.5.3

# Leitura de variáveis de ambiente (.env local)
python-dotenv==1.0.1
